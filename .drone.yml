kind: pipeline
type: docker
name: default

steps:
  - name: build_deploy
    privileged: true
    image: node:latest
    volumes:
      - name: dazzle
        path: /dazzle:/dazzle
    commands:
      - npm install
      - npm run build
      - ls .
      - ls /
      - cp -r ./build/* /dazzle/
      - ls /dazzle/
      - cat /dazzle/index.html
